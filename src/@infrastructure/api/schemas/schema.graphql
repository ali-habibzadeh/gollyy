type Ticket @model {
  id: String!
  numbers: [Int]!
  createdAt: AWSDateTime!
}

input TicketInput {
  numbers: [Int]!
}

type Query {
  getTickets: [Ticket]
}

type Mutation {
  createTicket(numbers: TicketInput!): Ticket
}
